EXPO_PUBLIC_APPWRITE_PROJECT_ID=
EXPO_PUBLIC_APPWRITE_PROJECT_NAME=
EXPO_PUBLIC_APPWRITE_ENDPOINT=
EXPO_PUBLIC_APPWRITE_DB_ID=
EXPO_PUBLIC_APPWRITE_BUNDLE_ID= [for iOS]
EXPO_PUBLIC_APPWRITE_PACKAGE_NAME= [for android]
EXPO_PUBLIC_APPWRITE_COL_NOTES_ID= [table name]

# For self-verification: full URL to your /verify page (e.g. https://yourapp.vercel.app/verify).
# Required for verification emails. Add this hostname to Appwrite Auth → Settings → Platform list.
# For development, use http://localhost:8081/verify, or whatever port your app uses.
EXPO_PUBLIC_VERIFY_URL=
